// import sinon, { assert } from 'sinon';
// import pool from 'common/database';
// import UserModel from 'models/UserModel';
// import * as mock from 'tests/controllers/users.mock';

describe('UserModel', () => {
    // afterEach(() => { sinon.restore(); });

    // it('should get all users from database', async () => {
    //     const poolStub = sinon.stub(pool, 'execute').resolves([mock.users] as any);
    //     await UserModel.getAll();
    //     assert.calledOnceWithExactly(poolStub, 'SELECT * FROM ?' as any, ['users']);
    // });

    // it('should get user id 2 from database', async () => {
    //     const id = 2;
    //     const poolStub = sinon.stub(pool, 'execute').resolves([[mock.users[1]]] as any);
    //     await UserModel.getById(id);
    //     assert.calledOnceWithExactly(poolStub, 'SELECT * FROM ? WHERE id = ? LIMIT 1' as any, ['users', id]);
    // });
});
